import { connect } from "react-redux";
import Special from "./Special.jsx";
let mapStateToProps = (state) => {
    return {

    };
};

const SpecialContainer = connect(mapStateToProps)(Special);
export default SpecialContainer;